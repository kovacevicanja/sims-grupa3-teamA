<Window x:Class="BookingProject.View.OwnersView.EnterAccommodationRenovationDatesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingProject.View.OwnersView"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="EnterAccommodationRenovationDates" WindowStartupLocation="CenterScreen"  Height="600" Width="356">
    <Grid>
        <StackPanel Orientation="Vertical" VerticalAlignment="Center" Width="180">
            <Label>Pocetni datum</Label>
            <DatePicker DisplayDateStart="{x:Static sys:DateTime.Today}" SelectedDate="{Binding StartDate,Mode=OneWayToSource}" ></DatePicker>
            <Label Margin="0 10 0 0"  >Krajnji datum</Label>
            <DatePicker DisplayDateStart="{x:Static sys:DateTime.Today}" SelectedDate="{Binding EndDate, Mode=OneWayToSource}"></DatePicker>
            <Label Margin="0 15 0 0">Trajanje renoviranja</Label>
            <TextBox Height="20" Text="{Binding RenovationDuration}"></TextBox>
            <Button Margin="0 30 0 0" Height="25" Command="{Binding ShowCommand}">Prikazi</Button>
        </StackPanel>
    </Grid>
</Window>

<Window x:Class="BookingProject.View.Guest1View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingProject.View"
        mc:Ignorable="d"
        Title="Guest1" Height="450" Width="800">

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="350"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Label Width="100">Name:</Label>
            <Label Width="100">City:</Label>
            <Label Width="100">State:</Label>
            <Label Width="100">Type:</Label>
            <Label Width="110">Number of guests:</Label>
            <Label Width="200">Number of the days for reservation:</Label>
        </StackPanel>

        <StackPanel Grid.Row = "1" Orientation="Horizontal">
            <TextBox Width="90" Height="20" Margin="5"></TextBox>
            <TextBox Width="90" Height="20" Margin="5"></TextBox>
            <TextBox Width="90" Height="20" Margin="5"></TextBox>
            <ComboBox Width="90" Height="20" Margin="5"></ComboBox>
            <TextBox Width="90" Height="20" Margin="5"></TextBox>
            <TextBox Width="110" Height="20" Margin="5"></TextBox>
            <Button Content="Search" IsCancel="True" HorizontalAlignment="Right" VerticalAlignment="Top"  Margin="20, 5, 20, 5"
               Click="Button_Click_Search"/>
            <Button Content="Cancel search" IsDefault="True" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="20, 5, 20, 5"
              Click="Button_Click_Cancel_Search" />
        </StackPanel>

        <Grid Grid.Row="2">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>


            <DataGrid Margin="16, 12, 16, 16" SelectionMode="Single" AutoGenerateColumns="False" Name="AccommodationDataGrid" IsReadOnly="True" SelectionChanged="AccommodationDataGrid_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Width="60" Binding="{Binding Name}"></DataGridTextColumn>
                    <DataGridTextColumn Header="City" Width="60" Binding="{Binding Location.City}"></DataGridTextColumn>
                    <DataGridTextColumn Header="State" Width="60" Binding="{Binding Location.Country}"></DataGridTextColumn>
                    <DataGridTextColumn Header="Type" Width="60" Binding="{Binding Type}"></DataGridTextColumn>
                    <DataGridTextColumn Header="MaxNumOfGuests" Width="150" Binding="{Binding MaxGuestNumber}"></DataGridTextColumn>
                    <DataGridTextColumn Header="MinNumOfDaysForReservation" Width="200" Binding="{Binding MinDays}"></DataGridTextColumn>
                    <DataGridTextColumn Header="Cancellation limit" Width="200" Binding="{Binding CancelationPeriod}"></DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
            
        </Grid>

        <StackPanel Grid.Row="3">
            <Button Width="120" IsDefault="True" Click="Button_Click_Book">Book accommodation</Button>
        </StackPanel>
    </Grid>
</Window>

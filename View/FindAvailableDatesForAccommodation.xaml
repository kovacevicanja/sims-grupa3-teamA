<Window x:Class="BookingProject.View.FindAvailableDatesForAccommodation"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingProject.View"
        mc:Ignorable="d"
        Title="FindAvailableDatesForAccommodation" Height="450" Width="800">
    <Grid Background="AliceBlue">
        <Label FontSize="15" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="20">Sugestion for other dates:</Label>
        <DataGrid ItemsSource="{Binding Ranges}" AutoGenerateColumns="False" CanUserAddRows="False" Width="500" Height="300">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Start Date" Binding="{Binding StartDate, StringFormat={}{0:dd/MM/yyyy}}" Width="*" />
                <DataGridTextColumn Header="End Date" Binding="{Binding EndDate, StringFormat={}{0:dd/MM/yyyy}}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom">
            <Button Width="80" Height="30"  Margin="20">Reserve</Button>
            <Button Width="80" Height="30" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="20">Cancle</Button>
        </StackPanel>
    </Grid>
</Window>
